import path from 'path';
import { fileURLToPath } from 'url';

export const __filename = fileURLToPath(import.meta.url); //הקובץ הזה
export const __dirname = path.dirname(__filename); //הספריה של הקובץ הזה